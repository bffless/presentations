{
  "name": "workflow-talk",
  "private": true,
  "version": "0.0.0",
  "description": "Write YAML, get the UI — the Workflow harness and its implementations",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@presentations/theme": "workspace:*",
    "reveal.js": "^6.0.1"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
