{
  "name": "mcp",
  "private": true,
  "version": "0.0.0",
  "description": "Your backend is already an MCP server — building MCP servers out of BFFless proxy rules",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@presentations/theme": "workspace:*",
    "reveal.js": "^6.0.1"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
